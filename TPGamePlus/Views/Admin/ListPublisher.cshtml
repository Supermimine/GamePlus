@model IEnumerable<TPGamePlus.Domain.Entities.Publisher>
@{
    ViewData["Title"] = "Admin";
}

<head>
    <link rel="stylesheet" href="~/css/Admin.css">
</head>



<btn class="btn">
    <a asp-controller="Admin" asp-action="ManagePublisher">Ajouter un nouvel éditeur</a>
</btn>
<btn class="btn">
    <a asp-controller="Admin" asp-action="IndexAdmin">Page admin</a>
</btn>
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PublisherName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isMainShopFilter)
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PublisherName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.isMainShopFilter)
                </td>
                <td>
                    <a class="PointerCursor" onclick="ComfirmeDelete('/Admin/DeletePublisher/@item.PublisherID')">Supprimer</a> |
                    <a asp-controller="Admin" asp-action="EditPublisher" asp-route-id="@item.PublisherID">Modifier</a>
                </td>
            </tr>
        }
    </tbody>
</table>
